struct PS_INPUT
{
	float4 hpos : TEXCOORD;
};

struct PS_OUTPUT
{
	float4 colour : COLOR0;
};

PS_OUTPUT main( PS_INPUT fragment )
{
	PS_OUTPUT output;

	output.colour = float4( fragment.hpos.z / fragment.hpos.w, 0, 0, 1 );

	return output;
}